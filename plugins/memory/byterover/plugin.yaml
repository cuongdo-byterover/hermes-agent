name: byterover
version: 2.0.0-mono.0
description: "ByteRover — persistent knowledge tree backed by byterover-mono (.mjs scripts). Recall via prefetch(), curate via the brv_record agent tool."
external_dependencies:
  - name: node
    install: "https://nodejs.org/  (any modern Node.js — needed to run the bundled .mjs scripts)"
    check: "node --version"
